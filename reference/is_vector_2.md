# Test if object is a vector of length 2

Test if object is a vector of length 2

## Usage

``` r
is_vector_2(x)
```

## Arguments

- x:

  An object to test.

## Value

A logical.

## Examples

``` r
if (FALSE) { # \dontrun{
is_vector_2(c(100, 200))
is_vector_2(42)
} # }
```
