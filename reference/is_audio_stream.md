# Test if object is a audio_stream

Test if object is a audio_stream

## Usage

``` r
is_audio_stream(x)
```

## Arguments

- x:

  An object to test.

## Value

A logical.

## Examples

``` r
if (FALSE) { # \dontrun{
a <- load_audio_stream(44100L, 16L, 1L)
is_audio_stream(a)
} # }
```
