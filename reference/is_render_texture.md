# Test if object is a render_texture

Test if object is a render_texture

## Usage

``` r
is_render_texture(x)
```

## Arguments

- x:

  An object to test.

## Value

A logical.

## Examples

``` r
if (FALSE) { # \dontrun{
is_render_texture("not a render_texture")
} # }
```
