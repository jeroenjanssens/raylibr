# Check if running on web

Returns TRUE when running under Emscripten/webR, FALSE otherwise.

## Usage

``` r
is_web()
```

## Value

Logical

## Examples

``` r
if (FALSE) { # \dontrun{
is_web()
} # }
```
