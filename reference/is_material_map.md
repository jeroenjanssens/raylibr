# Test if object is a material_map

Test if object is a material_map

## Usage

``` r
is_material_map(x)
```

## Arguments

- x:

  An object to test.

## Value

A logical.

## Examples

``` r
if (FALSE) { # \dontrun{
is_material_map("not a material_map")
} # }
```
