# Test if object is a shader

Test if object is a shader

## Usage

``` r
is_shader(x)
```

## Arguments

- x:

  An object to test.

## Value

A logical.

## Examples

``` r
if (FALSE) { # \dontrun{
s <- load_shader("", "shader.fs")
is_shader(s)
} # }
```
