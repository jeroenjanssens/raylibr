# Test if object is a vr_device_info

Test if object is a vr_device_info

## Usage

``` r
is_vr_device_info(x)
```

## Arguments

- x:

  An object to test.

## Value

A logical.

## Examples

``` r
if (FALSE) { # \dontrun{
is_vr_device_info(x)
} # }
```
