# Test if object is a ray_collision

Test if object is a ray_collision

## Usage

``` r
is_ray_collision(x)
```

## Arguments

- x:

  An object to test.

## Value

A logical.

## Examples

``` r
if (FALSE) { # \dontrun{
is_ray_collision("not a ray_collision")
} # }
```
