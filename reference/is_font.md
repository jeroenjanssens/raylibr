# Test if object is a font

Test if object is a font

## Usage

``` r
is_font(x)
```

## Arguments

- x:

  An object to test.

## Value

A logical.

## Examples

``` r
if (FALSE) { # \dontrun{
f <- get_font_default()
is_font(f)
} # }
```
