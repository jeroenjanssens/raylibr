# Test if object is a camera_2d

Test if object is a camera_2d

## Usage

``` r
is_camera_2d(x)
```

## Arguments

- x:

  An object to test.

## Value

A logical.

## Examples

``` r
if (FALSE) { # \dontrun{
is_camera_2d("not a camera_2d")
} # }
```
