# Test if object is a music

Test if object is a music

## Usage

``` r
is_music(x)
```

## Arguments

- x:

  An object to test.

## Value

A logical.

## Examples

``` r
if (FALSE) { # \dontrun{
m <- load_music_stream("music.mp3")
is_music(m)
} # }
```
