# Test if object is a vector of length 4

Test if object is a vector of length 4

## Usage

``` r
is_vector_4(x)
```

## Arguments

- x:

  An object to test.

## Value

A logical.

## Examples

``` r
if (FALSE) { # \dontrun{
is_vector_4(c(0, 0, 0, 1))
is_vector_4(c(1, 2, 3))
} # }
```
