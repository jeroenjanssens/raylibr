# Test if object is a mesh

Test if object is a mesh

## Usage

``` r
is_mesh(x)
```

## Arguments

- x:

  An object to test.

## Value

A logical.

## Examples

``` r
if (FALSE) { # \dontrun{
m <- gen_mesh_cube(1.0, 1.0, 1.0)
is_mesh(m)
} # }
```
