# Test if object is a glyph_info

Test if object is a glyph_info

## Usage

``` r
is_glyph_info(x)
```

## Arguments

- x:

  An object to test.

## Value

A logical.

## Examples

``` r
if (FALSE) { # \dontrun{
is_glyph_info("not a glyph_info")
} # }
```
