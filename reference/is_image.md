# Test if object is a image

Test if object is a image

## Usage

``` r
is_image(x)
```

## Arguments

- x:

  An object to test.

## Value

A logical.

## Examples

``` r
if (FALSE) { # \dontrun{
i <- gen_image_color(100, 100, "blue")
is_image(i)
} # }
```
